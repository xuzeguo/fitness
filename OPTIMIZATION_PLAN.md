# 健身数据管理系统优化计划

## 项目概述

本文档记录了健身数据管理系统的优化方向和实施计划，旨在提升数据维护效率、代码可维护性和用户体验。

---

## 一、数据维护体验优化

### 1.1 问题分析

**当前痛点：**
- CSV 有 21 列，手动编辑容易出错且效率低
- 每次训练需填写所有动作列，即使未做也要标记"未做"或"–"
- 数据格式要求严格（如 `20kg×14×4`），输入不灵活
- 每次更新需手动运行 `npm run rebuild:data`

**影响：**
- 数据录入时间长（每次训练约 5-10 分钟）
- 容易遗漏或输错数据
- 心理负担重，降低记录意愿

### 1.2 优化方案

#### 方案 A：改用长表格式（推荐）

**设计：**
```csv
日期,动作名称,重量kg,次数,组数,备注
04-28,反哈克深蹲,40,12,4,
04-28,正哈克深蹲,40,12,4,
04-28,腿弯举,27,12,4,
```

**优势：**
- 只记录做了的动作，未做的自动留空
- 易于扩展新动作，无需修改表结构
- 便于按动作、日期筛选和统计
- 减少单行字符数，降低编辑出错率

**实施步骤：**
1. 创建新的 CSV 格式定义
2. 编写转换脚本（旧格式 → 新格式）
3. 更新 `rebuild-training-log-md.mjs` 以支持新格式
4. 保留旧格式兼容性（过渡期）

**工作量估算：** 2-3 天

#### 方案 B：创建 Web 表单页面（高优先级）

**功能设计：**
- 日期自动填充为今天
- 动作下拉选择（支持搜索）
- 自动显示上次该动作的重量/次数作为参考
- 支持快速复制上次训练记录
- 一键提交后自动更新 CSV 并重建数据

**技术方案：**
- 新增 `entry.html` 页面
- 使用 `localStorage` 缓存最近训练数据
- 提交时调用后端 API（需添加简单 Node.js 服务）或直接生成 CSV 下载

**实施步骤：**
1. 设计表单 UI（响应式布局）
2. 实现动作选择和数据填充逻辑
3. 添加数据验证（前端 + 后端）
4. 集成到现有构建流程

**工作量估算：** 3-4 天

---

## 二、代码质量与可维护性

### 2.1 脚本重构

**当前问题：**
- `rebuild-training-log-md.mjs` 有 438 行，职责过多
- 硬编码了大量历史数据（进店记录 47 条、围度记录 5 条）
- CSV 解析、数据转换、Markdown 生成混在一起
- 缺少单元测试

**重构方案：**

```
scripts/
├── lib/
│   ├── csv-parser.mjs        # CSV 解析（通用）
│   ├── data-validator.mjs    # 数据校验
│   ├── data-transformer.mjs  # 数据转换逻辑
│   └── markdown-builder.mjs  # Markdown 生成
├── rebuild-training-log-md.mjs  # 主入口（精简）
└── seed-csvs.mjs
```

**实施步骤：**
1. 提取 CSV 解析逻辑为独立模块
2. 提取数据校验逻辑（日期、数值范围）
3. 提取 Markdown 生成逻辑
4. 重写主脚本，组合各模块
5. 添加单元测试（使用 Vitest）

**工作量估算：** 2-3 天

### 2.2 添加数据校验

**校验规则：**

| 字段 | 规则 | 错误提示 |
|------|------|----------|
| 日期 | 格式 `MM-DD`，月份 01-12，日期 01-31 | "日期格式错误，应为 MM-DD" |
| 体重 | 40-200 kg | "体重超出合理范围" |
| 体脂率 | 5-50 % | "体脂率超出合理范围" |
| 重量 | 0-300 kg | "训练重量超出合理范围" |
| 次数 | 1-100 | "训练次数超出合理范围" |
| 组数 | 1-20 | "训练组数超出合理范围" |

**实施方式：**
```javascript
// data-validator.mjs
export function validateTrainingRow(row) {
  const errors = [];
  
  // 日期校验
  if (!/^\d{2}-\d{2}$/.test(row.日期)) {
    errors.push(`日期格式错误: ${row.日期}`);
  }
  
  // 体重校验
  const weight = parseFloat(row.体重kg);
  if (weight < 40 || weight > 200) {
    errors.push(`体重超出范围: ${weight}kg`);
  }
  
  return errors;
}
```

**工作量估算：** 1 天

### 2.3 配置文件统一管理

**创建 `config.json`：**
```json
{
  "exercises": [
    {
      "name": "坐立卷腹机",
      "category": "核心",
      "unit": "kg",
      "enabled": true
    },
    {
      "name": "卧推",
      "category": "胸",
      "unit": "kg",
      "enabled": true
    }
  ],
  "scoreWeights": {
    "strength": 0.5,
    "cardio": 0.35,
    "duration": 0.15
  },
  "validation": {
    "weight": { "min": 40, "max": 200 },
    "bodyFat": { "min": 5, "max": 50 },
    "trainingWeight": { "min": 0, "max": 300 }
  }
}
```

**优势：**
- 修改动作列表只需改一处
- 便于禁用/启用某些动作
- 权重可调整，无需改代码

**工作量估算：** 1 天

---

## 三、功能增强

### 3.1 多动作对比

**需求：**
- 在趋势页支持同时选择 2-4 个动作
- 在同一图表中对比重量/训练量趋势
- 使用不同颜色区分

**实施方案：**
```javascript
// 修改 trends.js
const exerciseSelects = [
  document.getElementById('exercise1'),
  document.getElementById('exercise2'),
  document.getElementById('exercise3')
];

function renderMultiExerciseChart(chart, data, exercises, metric) {
  const series = exercises.map((ex, idx) => ({
    name: ex,
    type: 'line',
    data: data.items.map(it => it.perExercise[ex]?.[metric]),
    color: COLORS[idx]
  }));
  // ...
}
```

**工作量估算：** 1-2 天

### 3.2 个人记录（PR）统计

**功能设计：**
- 新增 `records.html` 页面
- 显示每个动作的历史最大重量
- 显示达成日期和距今天数
- 高亮最近 7 天内的新记录

**数据结构：**
```javascript
{
  "坐立卷腹机": {
    "maxWeight": 45,
    "date": "04-15",
    "isRecent": true
  }
}
```

**工作量估算：** 1-2 天

### 3.3 训练频率分析

**功能设计：**
- 周汇总：每周训练次数、总时长、总训练量
- 月汇总：同上
- 热力图：类似 GitHub Contributions

**技术方案：**
- 使用 ECharts 日历热力图
- 颜色深度表示训练强度

**工作量估算：** 2 天

### 3.4 综合评分权重可配置

**实施方案：**
- 在趋势页添加权重调整滑块
- 实时更新评分曲线
- 保存到 `localStorage`

**工作量估算：** 0.5 天

---

## 四、用户体验优化

### 4.1 移动端适配

**当前问题：**
- 图表字体在手机上太小（15px）
- 表格横向滚动不流畅
- 按钮间距过小，易误触

**优化方案：**
```css
/* 媒体查询 */
@media (max-width: 768px) {
  .chart-container {
    font-size: 12px;
  }
  
  .echarts-label {
    font-size: 11px !important;
  }
  
  button {
    min-height: 44px; /* iOS 推荐触摸目标 */
    margin: 8px;
  }
}
```

**工作量估算：** 1 天

### 4.2 PWA 支持

**功能：**
- 离线可用（缓存静态资源和数据）
- 添加到主屏幕
- 后台同步（在线时自动更新数据）

**实施步骤：**
1. 创建 `manifest.json`
2. 添加 Service Worker
3. 配置缓存策略

**工作量估算：** 1-2 天

### 4.3 数据导出功能

**支持格式：**
- CSV（原始数据）
- Excel（带格式）
- PDF（报告）
- JSON（备份）

**实施方案：**
- 使用 `xlsx` 库生成 Excel
- 使用 `jsPDF` 生成 PDF

**工作量估算：** 1 天

---

## 五、实施路线图

### 阶段一：基础优化（1-2 周）

**目标：** 提升数据录入效率和代码质量

- [ ] 添加数据校验（1 天）
- [ ] 脚本重构（2-3 天）
- [ ] 创建配置文件（1 天）
- [ ] Web 表单页面（3-4 天）

**预期收益：**
- 数据录入时间减少 60%
- 代码可维护性提升 50%
- 数据错误率降低 80%

### 阶段二：功能增强（1-2 周）

**目标：** 增加数据分析能力

- [ ] 多动作对比（1-2 天）
- [ ] 个人记录统计（1-2 天）
- [ ] 训练频率分析（2 天）
- [ ] 综合评分权重可配置（0.5 天）

**预期收益：**
- 数据洞察深度提升 100%
- 训练计划制定更科学

### 阶段三：体验优化（1 周）

**目标：** 提升移动端和离线体验

- [ ] 移动端适配（1 天）
- [ ] PWA 支持（1-2 天）
- [ ] 数据导出功能（1 天）

**预期收益：**
- 移动端使用率提升 200%
- 离线可用性 100%

### 阶段四：长期规划（按需）

- [ ] 改用长表格式（2-3 天）
- [ ] 训练计划模板功能
- [ ] 社交分享功能
- [ ] 数据可视化大屏

---

## 六、技术债务清单

### 高优先级

1. **硬编码数据迁移**
   - 将脚本中的 47 条进店记录迁移到 CSV
   - 将 5 条围度记录迁移到 CSV
   - 预计节省 200+ 行代码

2. **动作列表重复定义**
   - 当前在 3 处定义（脚本、前端、CSV 表头）
   - 统一到 `config.json`

3. **缺少错误处理**
   - CSV 解析失败时无友好提示
   - 网络请求失败时无重试机制

### 中优先级

1. **性能优化**
   - 大数据量时图表渲染慢（>100 条记录）
   - 考虑虚拟滚动或分页

2. **代码重复**
   - 多处日期格式化逻辑
   - 提取为工具函数

### 低优先级

1. **依赖更新**
   - Vite 6.0.0 → 最新版
   - ECharts 考虑按需引入（减小体积）

---

## 七、风险评估

### 数据迁移风险

**风险：** 改用长表格式可能导致历史数据丢失

**缓解措施：**
- 保留旧格式兼容性（至少 3 个月）
- 提供自动转换脚本
- 备份原始数据

### 性能风险

**风险：** 数据量增长后（>500 条）页面加载变慢

**缓解措施：**
- 实施分页或虚拟滚动
- 按年份分割数据文件
- 添加性能监控

### 兼容性风险

**风险：** PWA 在某些浏览器不支持

**缓解措施：**
- 渐进增强，核心功能不依赖 PWA
- 提供降级方案

---

## 八、成功指标

### 效率指标

- 数据录入时间：从 5-10 分钟降至 2 分钟
- 数据错误率：从 5% 降至 <1%
- 代码行数：减少 20%

### 质量指标

- 单元测试覆盖率：>80%
- 代码复杂度：降低 30%
- 用户满意度：>90%

### 使用指标

- 移动端访问占比：从 10% 提升至 40%
- 数据录入频率：从每周 3 次提升至 5 次
- 功能使用率：新功能使用率 >60%

---

## 九、资源需求

### 开发资源

- 前端开发：15-20 天
- 测试：3-5 天
- 文档编写：2 天

### 技术栈

**新增依赖：**
- `vitest`：单元测试
- `xlsx`：Excel 导出
- `jsPDF`：PDF 导出

**可选依赖：**
- `workbox`：PWA Service Worker
- `chart.js`：备选图表库（如 ECharts 体积过大）

---

## 十、后续维护

### 定期任务

- 每月检查依赖更新
- 每季度性能审计
- 每半年用户反馈收集

### 文档维护

- 更新 README.md（新功能说明）
- 编写开发者文档（架构、API）
- 维护变更日志（CHANGELOG.md）

---

## 附录

### A. 相关文件清单

```
fitness/
├── OPTIMIZATION_PLAN.md       # 本文档
├── config.json                # 配置文件（待创建）
├── scripts/
│   ├── lib/                   # 模块化脚本（待创建）
│   ├── rebuild-training-log-md.mjs
│   └── seed-csvs.mjs
├── src/
│   ├── entry.js               # 数据录入页面（待创建）
│   ├── records.js             # 个人记录页面（待创建）
│   └── trends.js
└── public/
    ├── training-log.csv
    ├── body-metrics.csv
    ├── gym-visits.csv
    └── girth.csv
```

### B. 参考资料

- [ECharts 文档](https://echarts.apache.org/)
- [Vite 文档](https://vitejs.dev/)
- [PWA 最佳实践](https://web.dev/progressive-web-apps/)
- [CSV 格式规范](https://tools.ietf.org/html/rfc4180)

---

**文档版本：** v1.0  
**创建日期：** 2026-04-29  
**最后更新：** 2026-04-29  
**维护者：** guoxuze
