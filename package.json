{
  "name": "fitness",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync:standalone": "cp -f public/data.json public/report.md public/training-log.md public/training-log.csv public/goals.md standalone/ && cp -f src/styles/trends.css standalone/trends.css",
    "rebuild:data": "node scripts/rebuild-training-log-md.mjs && npm run sync:standalone"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "marked": "^15.0.0"
  }
}
